import Nav from "../components/Nav/nav";
import Footer from "../components/Footers/footer";
import Headings from "../components/Headers/header";
import { useState } from "react";


// Rentals page to populate available rentals and add them to cart. anything with rentalCart true
function Rentals(
    {
        // Props for data to then be passed to item_populator
    }
) {
    const [RentalList, setRental] = useState() 

}